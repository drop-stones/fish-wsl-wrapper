# Marker used to identify functions managed by wslwrap.fish
function _wslwrap_get_wrapper_marker --description "Get marker string for wslwrap-created wrapper functions"
    echo "[wslwrap::wrapper]"
end
